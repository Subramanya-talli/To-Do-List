<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Css/style.css">
    <title>Edit Task</title>
</head>

<body>
    <main>
        <div class="outer-container editTask">
            <div class="task-list">
                <form action="/api/tasks/update/<%= task._id %>?_method=PUT" method="POST" class="edit-form">
                    <input type="text" name="task" value="<%= task.task %>" required>
                    <button type="submit">Update</button>
                </form>
            </div>
            <div class="Cancel-Btn">
                <a href="/">Cancel</a>
            </div>
        </div>
    </main>
</body>

</html>